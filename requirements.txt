# ── Core framework ──
langgraph>=0.2.0
langchain-community>=0.2.0
langchain-tavily>=0.1.0

# ── Search / retrieval ──
arxiv>=2.1.0

# ── LLM backends ──
mlx-lm>=0.19.0           ; sys_platform == "darwin"
huggingface_hub>=0.23.0   ; sys_platform == "darwin"
requests>=2.31.0          # Ollama REST API (cross-platform)

# ── Checkpointing ──
langgraph-checkpoint-sqlite>=1.0.0

# ── Experiment deps (used by generated code) ──
numpy>=1.26.0
torch>=2.2.0

# ── Utilities ──
python-dotenv>=1.0.0
